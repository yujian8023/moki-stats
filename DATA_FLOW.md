# 📊 数据流动与更新流程

## 🔄 完整数据流

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Actions (每小时第 6 分钟)            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 抓取新竞赛                                           │
│  node src/fetch.js --incremental                            │
│  - 获取 OPEN/LIVE 状态的竞赛                                   │
│  - 保存到 docs/data/contests/{_id}.json                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 检查已结束的竞赛                                      │
│  node src/leaderboard.js --check-finished                   │
│  - 检查 endTime < now - 8 分钟的竞赛                           │
│  - 检查 leaderboard 是否已存在                                │
│  - 如果不存在，加入待抓取队列                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 抓取 Leaderboard                                     │
│  fetchLeaderboardsBatch()                                   │
│  - 每批 5 个，批间延迟 10 秒                                      │
│  - 请求间隔 1.5 秒（避免限流）                                   │
│  - 保存到 docs/data/leaderboards/{_id}.json                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 生成统计报告                                          │
│  node src/generate-daily.js                                 │
│  ├─ 昨日统计：如果不存在则生成（历史数据不变）                   │
│  ├─ 今日统计：每次都重新生成（包含最新 leaderboard）✅          │
│  └─ 7 天汇总：每次都更新（滚动窗口）                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: 提交并推送                                            │
│  git add -A && git commit && git push                       │
│  GitHub Pages 自动部署（1-2 分钟）                               │
└─────────────────────────────────────────────────────────────┘
```

## ⏰ 时间线示例（3 月 9 日）

| 时间 | 事件 | 操作 | 数据变化 |
|------|------|------|----------|
| **00:00** | 跨天 | 生成 `daily/2026-03-09.json` | 新今日文件 |
| **06:06** | 每小时任务 | 抓取新竞赛 + leaderboard | 今日统计更新 |
| **08:30** | 竞赛 A 结束 | - | - |
| **08:38** | 8 分钟后 | 下次任务抓取 A 的 leaderboard | 新增 1 个文件 |
| **10:00** | 竞赛 B 结束 | - | - |
| **10:06** | 每小时任务 | 抓取 B 的 leaderboard | 新增 1 个文件 |
| **10:06** | 生成统计 | **重新生成**今日统计 | ✅ 包含 A+B |
| **12:06** | 每小时任务 | 检查新数据 | 无新数据 |
| **12:06** | 生成统计 | **重新生成**今日统计 | ✅ 数据不变 |
| **14:00** | 竞赛 C 结束 | - | - |
| **14:08** | 8 分钟后 | 下次任务抓取 C 的 leaderboard | 新增 1 个文件 |
| **14:06** | 每小时任务 | 抓取 C 的 leaderboard | 新增 1 个文件 |
| **14:06** | 生成统计 | **重新生成**今日统计 | ✅ 包含 A+B+C |
| **...** | ... | ... | ... |
| **23:59** | 一天结束 | - | 今日数据完整 |
| **00:00** | 跨天 (3.10) | 生成 `daily/2026-03-10.json` | 新今日文件 |

## ✅ 关键修复

### 修复前 ❌
```javascript
// 如果今日统计已存在，就跳过
if (fs.existsSync(todayPath)) {
  console.log('⏭️  今日统计已存在，跳过');
  // ❌ 问题：新 leaderboard 不会更新到今日统计！
}
```

**问题场景：**
```
10:00 → 生成今日统计（10 个 leaderboard）
12:00 → 抓取新 leaderboard（第 11 个）
12:06 → 检测到今日统计已存在，跳过 ❌
结果：今日统计还是只有 10 个 leaderboard！
```

### 修复后 ✅
```javascript
// 昨日统计只在不存在时生成
if (!fs.existsSync(yesterdayPath)) {
  generateStatsReport('daily', yesterday);
}

// 今日统计每次都重新生成（包含最新 leaderboard）
generateStatsReport('daily', today);  // ✅ 每次都更新

// 7 天汇总每次都更新
generateStatsReport('weekly');  // ✅ 滚动窗口
```

**修复后场景：**
```
10:00 → 生成今日统计（10 个 leaderboard）
12:00 → 抓取新 leaderboard（第 11 个）
12:06 → 重新生成今日统计 ✅
结果：今日统计包含 11 个 leaderboard！✅
```

## 📁 文件更新策略

| 文件类型 | 更新策略 | 说明 |
|----------|----------|------|
| `contests/{_id}.json` | 增量添加 | 新竞赛才添加 |
| `leaderboards/{_id}.json` | 增量添加 | 结束后 8 分钟添加 |
| `stats/daily/YYYY-MM-DD.json` | **今日：每次更新**<br>**昨日：仅生成一次** | 今日数据实时刷新 |
| `stats/summary.json` | 每次更新 | 7 天滚动窗口 |

## 🎯 数据一致性保证

### ✅ 今日统计
- **每次都重新计算**（基于所有 leaderboard 文件）
- **包含最新数据**（刚抓取的 leaderboard 立即生效）
- **无缓存问题**（不依赖旧文件）

### ✅ 昨日统计
- **只在跨天时生成**（历史数据不变）
- **避免重复计算**（节省资源）
- **数据完整性**（生成后不再修改）

### ✅ 7 天汇总
- **每次都重新计算**（滚动窗口）
- **自动包含最新 7 天**（跨天自动更新范围）
- **自动排除过期数据**（第 8 天自动移除）

## 🔍 验证方法

### 检查今日统计是否更新
```bash
# 查看今日统计的生成时间
curl https://yujian8023.github.io/moki-stats/data/stats/daily/2026-03-09.json | jq '.generatedAt'

# 查看 leaderboard 数量
curl https://yujian8023.github.io/moki-stats/data/stats/daily/2026-03-09.json | jq '.leaderboardsAnalyzed'
```

### 检查 leaderboard 是否被统计
```bash
# 查看最新的 leaderboard 文件
ls -lt docs/data/leaderboards/ | head -5

# 检查是否包含在今日统计中
# （统计的 leaderboardsAnalyzed 应该等于 leaderboard 文件数）
ls docs/data/leaderboards/*.json | wc -l
```

### 监控工作流
```bash
# 查看最近的运行记录
https://github.com/yujian8023/moki-stats/actions

# 检查是否有错误
# 查看 "Generate statistics" 步骤的日志
```

---

## 🎉 总结

**修复后的流程保证：**

1. ✅ **新 leaderboard 立即生效** - 抓取后立即包含在今日统计中
2. ✅ **今日统计实时更新** - 每小时重新生成，包含最新数据
3. ✅ **昨日数据保持不变** - 跨天后不再修改
4. ✅ **7 天汇总自动滚动** - 自动包含最新 7 天

**数据增长完全自动化！** 🚀
